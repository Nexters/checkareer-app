<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nexters.checkareer">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".CheckareerApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Checkareer">
        <activity
            android:name=".presentation.ui.createprofile.CreateProfileActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.ui.onboarding.OnBoardingActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.ui.splash.SplashActivity"
            android:exported="true"
            android:noHistory="true"
            android:theme="@style/Theme.App.Starting">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".presentation.ui.home.HomeActivity"
            android:exported="true" />
    </application>

</manifest>